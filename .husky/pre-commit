# Run prettier (see package.json)
npx lint-staged

ng lint

# Update the Claude mirror of the agent config when sources changed
if git diff --cached --name-only | grep -qE '^(\.agents/skills/|\.cursor/mcp\.json$|AGENTS\.md$)'; then
  npm run sync:agent-config
  git add .claude/skills .mcp.json
fi
