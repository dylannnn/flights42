Before changing application or library code here, read `docs/architecture.md` and apply the architecture rules.

If the change touches state management, also read `docs/signal-store.md` when it exists.

Do not bypass documented domain boundaries. Prefer small, focused changes.
